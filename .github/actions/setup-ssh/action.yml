name: 'Setup SSH Authentication'
description: 'Configure SSH authentication for accessing private GitHub repositories'

runs:
  using: "composite"
  steps:
    - name: Setup SSH for private dependencies
      shell: bash
      run: |
        mkdir -p ~/.ssh
        chmod 700 ~/.ssh
        
        # Write the SSH key (preserve exact format)
        echo "${{ env.SSH_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        
        # Add GitHub to known hosts
        ssh-keyscan -t rsa github.com >> ~/.ssh/known_hosts
        
        # Test the key format
        ssh-keygen -y -f ~/.ssh/id_rsa > /dev/null
        
        # Start SSH agent and add key
        eval "$(ssh-agent -s)"
        ssh-add ~/.ssh/id_rsa
        
        # Configure Git URL rewriting
        git config --global url."git@github.com:".insteadOf "https://github.com/"
        git config --global url."git@github.com:".insteadOf "ssh://git@github.com/"